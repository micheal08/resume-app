import { Routes } from '@angular/router';
import { ResumeMatcherComponent } from './resume-matcher/resume-matcher.component';

export const routes: Routes = [
  { path: '', component: ResumeMatcherComponent },
  // Add more routes here if needed
];
